Triage all unprocessed markdown files in the Obsidian inbox.

## Instructions

1. Find all `.md` files in the inbox directory (ask user for path if not known)
2. For each file:
   - Apply the inbox-classifier skill to analyze and classify the content
   - Add appropriate frontmatter tags (personal/professional)
   - Save the updated file
3. Report summary of classifications applied

Use the classification rules defined in `.claude/skills/inbox-classifier.md`.
